import 'package:flutter/material.dart';

class noteDasboard extends StatelessWidget {
  const noteDasboard({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
